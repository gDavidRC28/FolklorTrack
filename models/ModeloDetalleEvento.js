export class ModeloDetalleEvento {
    constructor(id, titulo, fecha, lugar, detalles) {
      this.id = id;
      this.titulo = titulo;
      this.fecha = fecha;
      this.lugar = lugar;
      this.detalles = detalles;
    }
}