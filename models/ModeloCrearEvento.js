export default class ModeloCrearEvento {
    constructor(titulo, detalles, fecha, lugar) {
      this.titulo = titulo;
      this.detalles = detalles;
      this.fecha = fecha;
      this.lugar = lugar;
    }
  }